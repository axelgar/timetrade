<section class="trades">
  <h2>My Requests</h2>
  <ul>
    {{#each trades}}
      {{#unless this.isRejected}}
        {{#unless this.isConfirmed}}
          <div class="card-container">
            <div class="image">
              <img src="{{this.service.url}}" alt="{{this.service.name}}">
            </div>
            <li class="card">
              <h3 class="name">{{this.service.name}}</h3>
               <img src="../images/hourglass.png" class="cost-icon" alt="time"><p class="time">{{this.service.time}}</p>
              <p class="owner">Consumer: {{this.consumer.username}}</p>
              <a href="https://api.whatsapp.com/send?phone={{this.consumer.contact}}"><img src="/images/message-closed-envelope.png" class="message" alt="message-icon">Contact the consumer</a>
              <p class="state">State: {{this.providerState}}</p>
            </li>
          {{#unless this.isAccepted}}
            <form action="/trades/{{this.id}}/accept" method="post" >
              <div class="field submit">
                <button class="button accept">Accept</button>
              </div>
            </form>
          {{/unless}}
          {{#if this.isAccepted}}
            <form action="/trades/{{this.id}}/confirm" method="post" >
              <div class="field submit">
                <button class="button confirm">Confirm</button>
              </div>
            </form>
          {{/if}}
            <form action="/trades/{{this.id}}/reject" method="post" >
              <div class="field submit">
                <button class="button reject">Reject</button>
              </div>
            </form>
          </div>
        {{/unless}}
      {{/unless}}
    {{/each}}
  </ul>
</section>